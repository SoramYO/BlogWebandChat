<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign in</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="/static/css/main.css">
</head>

<body>
    <div class="body-background">
        <div class="container-fluid d-flex justify-content-center align-items-center h-100">
            <form id="signinform" role="form" method="POST" action="/admin/signin">

                <% if (data && data.error) { %>
                    <div id="signinalert" class="alert alert-danger">
                        <p>Error:</p>
                        <span>
                            <%= data.error %>
                        </span>
                    </div>
                    <% } %>

                        <% if (data && data.success) { %>
                            <div id="signinalert" class="alert alert-success">
                                <p>Success:</p>
                                <span>
                                    <%= data.success %>
                                </span>
                            </div>
                            <% } %>

                                <div class="card p-3 text-center py-4">
                                    <h4>Login</h4>

                                    <div class="mt-3 px-3">
                                        <input class="form-control" name="email" placeholder="E-mail">
                                    </div>
                                    <div class="mt-3 px-3">
                                        <input class="form-control" name="password" placeholder="Password"
                                            type="password">
                                    </div>
                                    <div class="mt-3 d-grid px-3">
                                        <button class="btn btn-primary btn-block btn-signup text-uppercase"
                                            type="submit">
                                            Login
                                        </button>
                                    </div>
                                    <div>
                                        <span>You not have an account?</span>
                                        <a href="/admin/signup" class="text-decoration-none">Signup</a>
                                    </div>
                                </div>

            </form>
        </div>
    </div>
</body>

</html>